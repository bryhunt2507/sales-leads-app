import MainHome from './MainHome.jsx'

function App() {
  // For now, force the CRM homepage to be the only view.
  // Once this works in production, we can re-add Entry/Admin switching.
  return <MainHome />
}

export default App
